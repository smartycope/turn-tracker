# turn-tracker
A simple HTML/JS script to keep track of turns during board games

# Dev Commands
- `npm run dev` - Run the dev server
- `npm run build` - Build the production version
- `npm run deploy` - Deploy to GitHub Pages (auto builds)
